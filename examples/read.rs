// extern crate pix;

fn main() {
/*    match png_pong::decode32_file("tests/test.png") {
        Ok(image) => println!(
            "Decoded image {} x {} and the first pixel's value is {:?}",
            image.width(),
            image.height(),
            image.as_slice()[0]
        ),
        Err(reason) => println!("Could not load, because: {}", reason),
    }*/
}
