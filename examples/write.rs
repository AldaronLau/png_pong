// use std::path::Path;

fn main() {
/*    let path = &Path::new("write_test.png");

    let image = [255u8, 0, 0, 0, 255, 0, 0, 0, 255, 0, 99, 99];

    let raster: pix::Raster<pix::Rgba8> =
        pix::RasterBuilder::new().with_u8_buffer(2, 2, &image[..]);

    // encode_file takes the path to the image, a u8 array,
    // the width, the height, the color mode, and the bit depth
    if let Err(e) =
        png_pong::encode_file(path, &raster, png_pong::ColorType::RGB, 8)
    {
        panic!("failed to write png: {:?}", e);
    }

    println!("Written to {}", path.display());*/
}
