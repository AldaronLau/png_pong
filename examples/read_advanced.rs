/*extern crate pix;

use std::path::Path;
*/
fn main() {
/*    let path = Path::new("tests/test.png");

    let mut state = png_pong::State::new();

    match state.decode_file(&path) {
        Ok(image) => match image {
            png_pong::Image::RGBA(bitmap) => {
                println!(
                    "Decoded image {} x {}",
                    bitmap.width(),
                    bitmap.height()
                );
                println!("The first pixel is {:?}", bitmap.as_slice()[0]);
                println!("The raw bytes are {:?}", bitmap.as_slice());
            }
            _x => println!("Decoded some other image format"),
        },
        Err(reason) => {
            println!("Could not load {}, because: {}", path.display(), reason)
        }
    }*/
}
