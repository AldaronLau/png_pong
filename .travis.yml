language: rust
sudo: false

cache: cargo

rust:
  - beta
  - nightly

matrix:
    fast_finish: true
    include:
      - os: linux
        rust: stable
        compiler: gcc

      - os: windows
        rust: stable

      - os: osx
        rust: stable

script:
  - cargo update
  - cargo test --all --all-features
